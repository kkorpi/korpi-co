---
type Props = {
  href: string;
};

const { href } = Astro.props;
const isExternal = /^https?:\/\//.test(href);
const attrs = isExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {};
---
<a href={href} {...attrs}><span class="link-text"><slot /></span></a>
